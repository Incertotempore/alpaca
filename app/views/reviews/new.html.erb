<div class="form-container radio-buttons-inline">
<%= simple_form_for [@item, @review] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating, collection: [ 0, 1, 2, 3, 4, 5 ], as: :radio_buttons %>

  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>
</div>
